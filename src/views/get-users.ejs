<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Get all users</title>
</head>
<body>
    <h1>Get all users from DB</h1>
    <style>
        table, th, td {
          border:1px solid black;
          border-collapse: collapse;
          text-align: center;
        }

        .btn-edit {
            outline: none;
            border: none;
            background: #ABC4AA;
            height: 25px;
            width: 60px;
            border-radius: 5px;
            cursor: pointer;
            margin: 3px;
        }

        .btn-delete {
            outline: none;
            border: none;
            background: #DF2E38;
            height: 25px;
            width: 60px;
            border-radius: 5px;
            cursor: pointer;
            margin: 3px;
        }

    </style>
        
        <table style="width:50%">
          <tr>
            <th>Email</th>
            <th>Adress</th>
            <th>Phone number</th>
            <th>User update</th>
          </tr>
          <% for(let i=0; i < usersTable.length; i++) { %>
            <tr>
              <td><%= usersTable[i].email %></td>
              <td><%= usersTable[i].address %></td>
              <td><%= usersTable[i].phoneNumber %></td>
              <td>
                <a href="/user-edit?id=<%=usersTable[i].id%>" class="btn-edit" type="button">Edit</a>
                <a href="/user-delete?id=<%usersTable[i].id%>" class="btn-delete" type="button">Delete</a>
              </td>
            </tr>
         <% } %>
        </table>

</body>
</html>